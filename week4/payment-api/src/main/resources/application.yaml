server:
  port: 8081

spring:
  application:
    name: payment-api

# 로깅 설정
logging:
  level:
    root: INFO
    com.example.payment: DEBUG
  pattern:
    console: "%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n"
